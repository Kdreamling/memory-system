# 补充修改（贴到对应位置）

## API 补充

### milestones 排序调整
```
排序：按 date 升序，同日期按 created_at 升序
```

### promises 返回字段补充
```
返回字段：id, content, promised_by, date, status, created_at
```

### wishlists 返回字段补充
```
返回字段：id, content, wished_by, date, status, created_at
```

## 前端补充

### 承诺人/许愿人显示映射
前端渲染时，对 promised_by / wished_by 做以下映射：
- "Dream" → "Dream 🌙"
- "Claude" → "晨 ☀️"
- "一起" → "一起 💕"

注意：数据库存的是 "Claude" 不是 "晨"，前端展示时需要转换。

### 导航栏处理
新增3个页面后导航共6项，移动端可能拥挤。建议：
- 方案A：将"编年史、承诺墙、心愿单"归入一个下拉菜单"我们的空间"
- 方案B：导航栏改为可横向滚动
- 由开发者根据现有样式选择最合适的方案

### promises / wishlists 卡片上显示日期
承诺卡片显示 date 字段（承诺日期），而非 created_at
心愿卡片同理
